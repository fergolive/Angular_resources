import { TrackscrollerDirective } from './trackscroller.directive';

describe('TrackscrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackscrollerDirective();
    expect(directive).toBeTruthy();
  });
});
